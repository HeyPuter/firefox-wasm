# Memory index

- [Gecko-wasm toolchain](gecko-wasm-toolchain.md) — how to build/run the emscripten wasm SpiderMonkey/Gecko, env quirks (binaryen shim, ABI flags, single-thread).
- [Gecko-wasm source patches](gecko-wasm-source-patches.md) — the in-tree firefox/ edits that make the emscripten build work.
- [Gecko-wasm runtime frontier](gecko-wasm-runtime-frontier.md) — engine links to a 174MB wasm; static-link recipe; xptcall (NS_InvokeByIndex + 247 stubs) is the central remaining blocker.
- [Gecko-wasm WebGL→canvas](gecko-wasm-webgl-canvas.md) — de-risked facts: emscripten WebGL2 presents to page canvas under PROXY_TO_PTHREAD; threading/present constraints.
- [Gecko-wasm GPU integration](gecko-wasm-gpu-integration.md) — WebRender→WebGL2→canvas wiring (files+status); pipeline runs on real GPU, content not yet visible (debugging).
- [Gecko-wasm GPU BGR swap](gecko-wasm-gpu-bgr-swap.md) — ?gpu=1 R↔B swap on raster images (WebGL2 has no texture swizzle/BGRA); FIXED via a shader-side swizzle (uColor0Swizzle uniform + swizzle_settings Some on emscripten).
- [Gecko-wasm performance](gecko-wasm-perf.md) — wasm-specific perf hotspots + fixes: FindFreeAddressSpace 1GiB memset, socket-poll busy-spin→event-driven; profiler tool; proxy-tax/stylo/JS levers.
- [Gecko-wasm full chrome](gecko-wasm-full-chrome.md) — embed-chrome/ renders full Firefox front-end (browser.xhtml) headless; sites load in tabs; the non-obvious bring-up steps (appshell, GRE/APP split, non-remote tabs).
- [Gecko-wasm canvas passthrough](gecko-wasm-canvas-passthrough.md) — content WebGL on the host GPU via worker-local OffscreenCanvas (flag GECKO_GL_PASSTHROUGH); the silent EM_ASM-comma build-staleness bug.
- [Gecko-wasm wasm passthrough](gecko-wasm-wasm-passthrough.md) — guest WebAssembly routed to the host engine (content wasm was absent under --disable-jit); instantiate + export functions bridge; imports/streaming/memory open.
- [Gecko-wasm DOM mirror](gecko-wasm-dom-mirror.md) — headless mode (?mirror=1) that serializes the DOM to a native iframe; mouse+keyboard work after fixing a refresh-driver compositor sync-IPC hang.
- [Gecko-wasm mach build deadlock](gecko-wasm-build-mach-deadlock.md) — ./mach build hangs in the agent shell; build libxul via plain `make -C` subtrees + restrip-relink instead.
- [Gecko-wasm JS→WASM JIT](gecko-wasm-js-wasm-jit.md) — runtime JS→wasm-bytecode JIT (no native JIT); lowers hot numeric funcs, instantiates on host, hooks PBL+C++ interpreter call paths. M1 (straight-line) works.
- [Wasm-JIT Mode VS re-entry crash](wasm-jit-modevs-reentry-crash.md) — pre-existing intermittent OOB (~1/6 on DeltaBlue) in the DEFAULT JIT build; Mode VS wasm→wjhelp→JS::Call re-entry needs a CallNative-style exit frame in PortableBaselineInterpret.cpp.
- [Wasm-JIT polymorphic call IC](wasm-jit-polymorphic-call-ic.md) — N-way (4) poly call dispatch via SelectNumeric chain + call_indirect; correct + engaged on DeltaBlue (polyways=11) but perf-neutral. Real bottleneck = GetProp/SetElem helper boundary tax → next lever is Mode VS inline property access.
- [Wasm-JIT Mode VS GetProp poly + length](wasm-jit-modevs-getprop-poly-length.md) — the big DeltaBlue win: N-way polymorphic Mode VS GetProp + inline .length (string/dense-array). 1.35x -> ~2.5x over interpreter. A/B gates GECKO_WJVS_NOPOLYPROP / NOLEN.
- [Wasm-JIT Mode VS unbox](wasm-jit-modevs-unbox.md) — unboxed f64 operand stack + typed locals in mutating Mode VS. TWO correctness bugs found+FIXED 2026-06-18 (GetElem result repr mismatch; slot analysis ignored stores). tloc now CORRECT on 14 benches and made DEFAULT-ON (crypto ~3.16x). Disable via GECKO_WJVS_NOUNBOX / NOTYPEDLOC.
- [Wasm-JIT middle-end Phase A/B/F](wasm-jit-middleend-phaseA.md) — SSA IR substrate (GECKO_WJVS_IR; byte-identical parity PROVEN) + GVN redundant-load elim (GECKO_WJVS_GVN; correct but neutral, within-block CSE rare) + Phase F deopt-resume (GECKO_WJVS_FDEOPT): single-frame AND cross-frame/non-leaf resume WORKING via self-resume (bailing fn calls wjhelp(WJH_RESUME)→PBL, returns normal result; proven on richards incl. non-leaf TaskControlBlock.run via call_indirect; raytrace/crypto/navier correct). JitScript provisioned at compile via realm+ensureJitZoneExists. Open: splay crashes under FDEOPT (GC traces bad InterpreterFrame value via the js::Interpret→WasmJitRunCall entry path lacking exit-frame setup); perf (must bail rarely). All gated, default OFF, default build clean. ~9 Phase-F bugs fixed; see doc §3 STATUS.
- [Wasm-JIT richards analysis](wasm-jit-richards-analysis.md) — why the JIT is perf-NEUTRAL on richards (jit~=interpreter) vs deltablue 1.6-2x: richards is dispatch+allocation bound (megamorphic task.run, tiny methods, Packet alloc), not property/arith dense. Object-store inlining built (GECKO_WJVS_OBJSET, default off) — correct but neutral. A real 2x needs poly-SetProp + polymorphic/non-leaf inlining + lean call boundaries.
- [Wasm-JIT GetAliasedVar](wasm-jit-getaliasedvar.md) — Mode VS support for closed-over var reads (gWJCurEnv set/saved per WasmJitRunCall entry; needsSomeEnvironmentObject guard; aliased fns excluded from the fast-call table). Correct + landed but neutral on navier-stokes (regular-Array ceiling, not the missing opcode).
- [Wasm-JIT typed arrays](wasm-jit-typed-arrays.md) — typed-array element access (Float64Array/Int32Array): raw unboxed load/store, no boxing/barrier. The feature that hits the goal: verified ~10-12x JIT-on-vs-off on typed-array numeric kernels (core Octane caps ~3x; typed-array code is the 5-10x regime).
- [Wasm-JIT node harness + perf](wasm-jit-node-harness-perf.md) — embed-js/ SpiderMonkey-only node harness (~20s iter); the --no-liftoff measurement-noise fix (V8 wasm tiering); empirical TurboFan-era octane (JIT wins numeric/property crypto 3.3x, loses call/alloc richards 0.87x); TYPEDFIELD/TYPEDELEM shipped default-ON (+16% crypto). Remaining lever = allocation (constructors not Mode-VS-capable: InitProp→vsOK=false).
