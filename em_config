import os, shutil
EMSDK = os.environ.get('EMSDK', '/usr/lib/emsdk')
EMSCRIPTEN_ROOT = EMSDK + '/upstream/emscripten'
LLVM_ROOT = EMSDK + '/upstream/bin'
# emsdk bundles binaryen v117, which rejects the wasm features rustc/LLVM emit.
# Point at a complete system binaryen >= v129 instead (override with EM_BINARYEN_ROOT).
BINARYEN_ROOT = os.environ.get('EM_BINARYEN_ROOT', '/usr')
NODE_JS = shutil.which('node') or (EMSDK + '/node')
