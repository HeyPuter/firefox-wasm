{
  "name": "gecko-wasm-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Gecko (Firefox engine) compiled to WebAssembly: the libxul.js library and demo apps.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm --filter libxul.js build",
    "demo": "pnpm --filter embed-demo dev",
    "chrome": "pnpm --filter chrome-demo dev"
  }
}
